# Checking pip version:
pip install --upgrade pip


# For windows user:
pip install numpy
pip install soundfile

# For Linux user:
pip install numpy
pip install soundfile
sudo apt-get install python3-tk

# For mac os user:
pip install numpy
pip install soundfile

# check thinter in macos:
python3 -m tkinter
# if not installed:
brew install python-tk

# Run Codec software:
open cmd/power shell
python GUI.py